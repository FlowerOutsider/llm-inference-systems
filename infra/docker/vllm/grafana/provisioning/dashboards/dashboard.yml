apiVersion: 1

providers:
  - name: vllm
    orgId: 1
    folder: LLM Inference
    folderUid: llm-inference
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards